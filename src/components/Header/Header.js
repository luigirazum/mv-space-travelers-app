import React from 'react';
import NavBar from '../NavBar/NavBar';

const Header = () => (
  <>
    <header>
      <NavBar />
      <span>My profile</span>
    </header>
  </>
);

export default Header;
